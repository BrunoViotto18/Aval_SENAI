export interface Automovel{
    id: number,
    modelo: string
    preco: number
}

export interface Cliente{
    id: number
    nome: string
}

export interface Concessionaria{
    id: number
    nome: string
}
